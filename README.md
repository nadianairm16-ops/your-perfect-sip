# The Drink Dream

A cozy Starbucks-style custom drink builder built with React, Vite, TypeScript, Tailwind CSS, Zustand, Framer Motion, and React Router.

## Start the app

```bash
npm install
npm run dev
```

Open the local URL shown in the terminal.
